# ----------------------------------
# SERVER CONFIGURATION
# ----------------------------------
PORT=5000
NODE_ENV=development

# ----------------------------------
# DATABASE (MongoDB)
# ----------------------------------
# Replace 'localhost' with your Atlas URI for production
MONGO_URI=mongodb://localhost:27017/project_parallax

# ----------------------------------
# SECURITY (JWT)
# ----------------------------------
# Use a strong random string (e.g., generated via 'openssl rand -base64 32')
JWT_SECRET=your_super_secret_key_here_change_this
JWT_EXPIRE_IN=6h
JWT_COOKIE_EXPIRE=24

# ----------------------------------
# SOCKET.IO & CORS
# ----------------------------------
# The URL of your React Frontend (Vite usually runs on 5173)
# CLIENT_URL=http://localhost:5173
# CORS_ORIGIN=http://localhost:5173

# ----------------------------------
# REDIS (For ConnectionManager)
# ----------------------------------
# If using local Redis (Optional for dev, recommended for production scaling)
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=secret (Uncomment if needed)